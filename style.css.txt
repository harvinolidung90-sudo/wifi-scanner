/* Reset & Body */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    background-color: #1a1a2e;
    color: #fff;
    display: flex;
    justify-content: center;
    min-height: 100vh;
    padding-top: 50px;
}

/* Container Utama */
.container {
    width: 90%;
    max-width: 400px;
    background-color: #16213e;
    border-radius: 15px;
    padding: 20px;
    box-shadow: 0 10px 25px rgba(0,0,0,0.5);
}

.header {
    text-align: center;
    margin-bottom: 20px;
}

.header h1 {
    color: #0f3460;
    color: #e94560;
    font-size: 1.5rem;
}

/* Tombol Scan */
.scan-btn {
    width: 100%;
    padding: 12px;
    background-color: #e94560;
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    cursor: pointer;
    transition: background 0.3s;
}

.scan-btn:hover {
    background-color: #c72c41;
}

.scan-btn:disabled {
    background-color: #555;
    cursor: not-allowed;
}

/* Status Teks */
.status-text {
    text-align: center;
    font-size: 0.8rem;
    color: #888;
    margin-top: 10px;
    min-height: 20px;
}

/* Loading Animation */
.loader {
    border: 4px solid #f3f3f3;
    border-top: 4px solid #e94560;
    border-radius: 50%;
    width: 30px;
    height: 30px;
    animation: spin 1s linear infinite;
    margin: 20px auto;
}

.hide {
    display: none;
}

@keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}

/* Daftar Wi-Fi */
.wifi-list {
    list-style: none;
    margin-top: 20px;
}

.wifi-item {
    background-color: #0f3460;
    padding: 15px;
    margin-bottom: 10px;
    border-radius: 8px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    animation: slideIn 0.5s ease-out;
}

.network-name {
    font-weight: bold;
    font-size: 1rem;
}

.signal-strength {
    font-size: 0.8rem;
    padding: 4px 8px;
    border-radius: 4px;
    background-color: #222;
}

/* Kode Warna Kekuatan Sinyal */
.strength-3 { color: #4cd137; } /* Kuat */
.strength-2 { color: #fbc531; } /* Sedang */
.strength-1 { color: #e84118; } /* Lemah */

@keyframes slideIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}